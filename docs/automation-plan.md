# OpenClaw / Codex 安全自动化计划

允许自动化：搜集选题、总结公开资料、生成原创草稿、检查 SEO、添加文章草稿、更新工具数据、检查死链、生成 sitemap、创建 Pull Request。

禁止自动化：直接复制别人文章、洗稿、自动发布未经审核内容、自动投标、自动联系客户群发垃圾信息、抓取平台禁止抓取的数据。

所有新文章默认 `status=draft` 且 `noindex=true`。只有人工审核后进入 `review`，并通过发布脚本确认后才能变成 `published`。
