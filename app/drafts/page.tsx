import { notFound } from "next/navigation"; import { getAllPosts } from "@/lib/blog"; export default function DraftsPage(){ if(process.env.NODE_ENV!=="development")notFound(); const drafts=getAllPosts(true).filter((p)=>p.status!=="published"); return <main className="mx-auto max-w-6xl px-4 py-12"><h1 className="text-3xl font-bold">草稿预览</h1><div className="mt-6 space-y-3">{drafts.map((p)=><div key={p.slug} className="rounded-lg border p-4"><strong>{p.title}</strong><p className="text-sm text-gray-600">{p.status} · batch {p.publishBatch} · noindex {String(p.noindex)}</p></div>)}</div></main> }
