import { getAllPosts } from "@/lib/blog"; import { Card } from "@/components/Card"; export const metadata={title:"新手教程",description:"AI 接单、Codex、Upwork 和自由职业教程。",alternates:{canonical:"/blog"}}; export default function BlogPage(){ const posts=getAllPosts(false); return <main className="mx-auto max-w-6xl px-4 py-12"><h1 className="text-3xl font-bold">新手教程</h1><p className="mt-3 text-gray-600">这里只展示已人工审核发布的文章，草稿不会进入公开列表。</p><div className="mt-8 grid gap-4 md:grid-cols-3">{posts.map((p)=><Card key={p.slug} title={p.title} description={p.description} href={`/blog/${p.slug}`}/>)}</div></main> }
